---
// ContactForm -- ported verbatim from the original React component `oa()`.
// contact form shell -- fields are still built by JS (contact.ts), coupled to a 3D pointer hit-test proxy
// DOM structure/classes are load-bearing: the engine (src/scripts) selects
// these exact elements by class/attribute. Do not rename without updating it.
---
<div class="cf" aria-hidden="true" inert=""><svg class="cf-orb" fill="none" xmlns="http://www.w3.org/2000/svg"><ellipse></ellipse><ellipse></ellipse><ellipse></ellipse></svg><div class="cf-gl"></div><div class="cf-lead"><b>The application</b><p>Tell us what you sell and where you want to grow. Every application is read, and when the model fits we answer within a week.</p></div><div class="cf-in"></div></div>
