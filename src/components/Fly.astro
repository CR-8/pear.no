---
// Fly -- ported verbatim from the original React component `$i()`.
// the flysky canvas + its backdrop
// DOM structure/classes are load-bearing: the engine (src/scripts) selects
// these exact elements by class/attribute. Do not rename without updating it.
---
<canvas class="fly" aria-hidden="true"></canvas><div class="fly-w" aria-hidden="true"></div>
